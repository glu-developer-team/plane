# =============================================================================
# Operator config — nguồn để generate /opt/plane/plane.env
# (port từ bita-plane/.env.example)
#
#   cp deploy/operator.env.example /opt/plane/operator.env
#   vim /opt/plane/operator.env
#   OPERATOR_ENV=/opt/plane/operator.env ./deploy/bin/generate-plane-env.sh
# =============================================================================

# -----------------------------------------------------------------------------
# Domain & URL
# -----------------------------------------------------------------------------
APP_DOMAIN=plane.bita.tokyo
WEB_URL=https://plane.bita.tokyo
CORS_ALLOWED_ORIGINS=https://plane.bita.tokyo

# -----------------------------------------------------------------------------
# Nginx reverse proxy
# -----------------------------------------------------------------------------
USE_NGINX_PROXY=true
NGINX_CONF_PATH=/etc/nginx/conf.d/plane.conf
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
PLANE_UPSTREAM_HOST=127.0.0.1
PLANE_UPSTREAM_PORT=8080
NGINX_SSL_ENABLED=true
SSL_CERT_PATH=/etc/letsencrypt/live/plane.bita.tokyo/fullchain.pem
SSL_KEY_PATH=/etc/letsencrypt/live/plane.bita.tokyo/privkey.pem
CERT_EMAIL=duy_kieu@bita.jp

# -----------------------------------------------------------------------------
# Docker images (self-hosted build — deploy.sh override APP_RELEASE từ git SHA)
# -----------------------------------------------------------------------------
DOCKERHUB_USER=plane-local
APP_RELEASE=develop

# -----------------------------------------------------------------------------
# PostgreSQL
# USE_BUNDLED_DB=true  → container plane-db
# USE_BUNDLED_DB=false → EXTERNAL_DATABASE_URL
# -----------------------------------------------------------------------------
USE_BUNDLED_DB=true
POSTGRES_HOST=plane-db
POSTGRES_PORT=5432
POSTGRES_USER=plane
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=plane
EXTERNAL_DATABASE_URL=

# -----------------------------------------------------------------------------
# Redis / Valkey
# -----------------------------------------------------------------------------
USE_BUNDLED_REDIS=true
REDIS_HOST=plane-redis
REDIS_PORT=6379
EXTERNAL_REDIS_URL=

# -----------------------------------------------------------------------------
# Object storage (MinIO / S3)
# -----------------------------------------------------------------------------
USE_BUNDLED_MINIO=true
AWS_ACCESS_KEY_ID=access-key
AWS_SECRET_ACCESS_KEY=CHANGE_ME_SECRET_KEY
AWS_S3_ENDPOINT_URL=http://plane-minio:9000
AWS_S3_BUCKET_NAME=uploads
AWS_REGION=
MINIO_ENDPOINT_SSL=0

# -----------------------------------------------------------------------------
# RabbitMQ
# -----------------------------------------------------------------------------
RABBITMQ_USER=plane
RABBITMQ_PASSWORD=CHANGE_ME_RABBITMQ_PASSWORD
RABBITMQ_VHOST=plane

# -----------------------------------------------------------------------------
# SMTP (Google App Password)
# -----------------------------------------------------------------------------
ENABLE_SMTP=true
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=1
EMAIL_USE_SSL=0
EMAIL_HOST_USER=your@gmail.com
EMAIL_HOST_PASSWORD=your-google-app-password
EMAIL_FROM="Plane <your@gmail.com>"
SKIP_ENV_VAR=0

# -----------------------------------------------------------------------------
# Security — để trống để script tự sinh
# -----------------------------------------------------------------------------
SECRET_KEY=
LIVE_SERVER_SECRET_KEY=

# -----------------------------------------------------------------------------
# Tùy chọn
# -----------------------------------------------------------------------------
DEBUG=0
GUNICORN_WORKERS=1
FILE_SIZE_LIMIT=5242880
API_KEY_RATE_LIMIT=6000/minute

# Bỏ comment nếu server ARM64
# DOCKER_PLATFORM=linux/amd64
